// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

model User {
  id          Int       @id @default(autoincrement())
  email       String    @unique
  name        String?
  password    String
  bio         String?
  avatar      String?
  phone       String?
  address     String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  role        Role      @default(USER) // 👈 New field for user type

  // Relation
  bookings    Booking[] // 👈 One-to-many relation (user can have many bookings)

    // 👇 Add these fields for OTP flow
  resetOtp        String?   
  resetOtpExpiry  DateTime? 
  resetOtpVerified Boolean? @default(false)
}

model Booking {
  id             Int       @id @default(autoincrement())
  userId         Int
  user           User      @relation(fields: [userId], references: [id])

  guestName      String
  email          String
  phone          String
  checkInDate    DateTime
  checkOutDate   DateTime
  numberOfPerson Int       // 👈 added this line
  status         String    @default("Pending")
  isVerified      Boolean  @default(false)   
  @@map("Booking")
  createdAt      DateTime  @default(now())
}


model VisitCounter {
  id        Int      @id @default(autoincrement())
  key       String   @unique
  count     BigInt   @default(0)
  updatedAt DateTime @updatedAt
}

model VisitLog {
  id           Int      @id @default(autoincrement())
  key          String
  visitorHash  String   // hashed IP + UA combo
  createdAt    DateTime @default(now())

  @@index([key, visitorHash])
}
